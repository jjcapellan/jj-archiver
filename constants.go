package archiver

const (
	// extPacked is the packed file extension
	extPacked = ".tar"
	// extCompressed is the compressed file extension
	extCompressed = ".gz"
	// extEncrypted is the encrypted file extension
	extEncrypted = ".crp"
)
